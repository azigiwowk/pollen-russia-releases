name: Publish latest alpha APK

on:
  workflow_dispatch:
  push:
    paths:
      - publish-request.txt

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      SOURCE_REPO: azigiwowk/pollen-russia-app
      RELEASE_REPO: azigiwowk/pollen-russia-releases

    steps:
      - name: Checkout public release repo
        uses: actions/checkout@v4

      - name: Verify source token
        run: |
          if [ -z "${{ secrets.SOURCE_REPO_PAT }}" ]; then
            echo "Missing SOURCE_REPO_PAT repository secret"
            exit 1
          fi

      - name: Find latest Android artifact
        env:
          GH_TOKEN_SOURCE: ${{ secrets.SOURCE_REPO_PAT }}
        run: |
          set -euo pipefail
          API="https://api.github.com/repos/$SOURCE_REPO/actions/artifacts?name=pollen-moscow-debug-apk&per_page=20"
          DATA="$(curl -fsSL             -H "Authorization: Bearer $GH_TOKEN_SOURCE"             -H "Accept: application/vnd.github+json"             -H "X-GitHub-Api-Version: 2022-11-28"             "$API")"

          ARTIFACT_ID="$(echo "$DATA" | jq -r '.artifacts | map(select(.expired == false)) | sort_by(.created_at) | reverse | .[0].id // empty')"
          if [ -z "$ARTIFACT_ID" ]; then
            echo "No non-expired pollen-moscow-debug-apk artifact found"
            exit 1
          fi
          echo "ARTIFACT_ID=$ARTIFACT_ID" >> "$GITHUB_ENV"

      - name: Download and unpack APK
        env:
          GH_TOKEN_SOURCE: ${{ secrets.SOURCE_REPO_PAT }}
        run: |
          set -euo pipefail
          mkdir -p /tmp/pollen-artifact
          curl -fsSL -L             -H "Authorization: Bearer $GH_TOKEN_SOURCE"             -H "Accept: application/vnd.github+json"             -H "X-GitHub-Api-Version: 2022-11-28"             "https://api.github.com/repos/$SOURCE_REPO/actions/artifacts/$ARTIFACT_ID/zip"             -o /tmp/pollen-artifact.zip
          unzip -o /tmp/pollen-artifact.zip -d /tmp/pollen-artifact
          APK="$(find /tmp/pollen-artifact -type f -name '*.apk' | head -n 1)"
          if [ -z "$APK" ]; then
            echo "APK not found inside artifact"
            exit 1
          fi
          cp "$APK" pollen-russia.apk

      - name: Read app version from private source
        env:
          GH_TOKEN_SOURCE: ${{ secrets.SOURCE_REPO_PAT }}
        run: |
          set -euo pipefail
          CONTENT_JSON="$(curl -fsSL             -H "Authorization: Bearer $GH_TOKEN_SOURCE"             -H "Accept: application/vnd.github+json"             -H "X-GitHub-Api-Version: 2022-11-28"             "https://api.github.com/repos/$SOURCE_REPO/contents/app/build.gradle.kts?ref=main")"
          echo "$CONTENT_JSON" | jq -r '.content' | tr -d '\n' | base64 -d > /tmp/app-build.gradle.kts

          VERSION_CODE="$(grep -Eo 'versionCode = [0-9]+' /tmp/app-build.gradle.kts | head -n1 | awk '{print $3}')"
          VERSION_NAME="$(grep -Eo 'versionName = "[^"]+"' /tmp/app-build.gradle.kts | head -n1 | cut -d'"' -f2)"

          if [ -z "$VERSION_CODE" ] || [ -z "$VERSION_NAME" ]; then
            echo "Could not parse app version"
            exit 1
          fi

          echo "VERSION_CODE=$VERSION_CODE" >> "$GITHUB_ENV"
          echo "VERSION_NAME=$VERSION_NAME" >> "$GITHUB_ENV"

      - name: Build public update manifest
        run: |
          set -euo pipefail
          SHA256="$(sha256sum pollen-russia.apk | awk '{print $1}')"
          APK_URL="https://raw.githubusercontent.com/$RELEASE_REPO/main/pollen-russia.apk"

          jq -n             --argjson versionCode "$VERSION_CODE"             --arg versionName "$VERSION_NAME"             --arg apkUrl "$APK_URL"             --arg sha256 "$SHA256"             '{
              versionCode: $versionCode,
              versionName: $versionName,
              apkUrl: $apkUrl,
              sha256: $sha256,
              notes: [
                ("Тестовая версия " + $versionName),
                "Обновление опубликовано автоматически из последней успешной Android-сборки"
              ]
            }' > version.json

      - name: Publish APK and manifest
        run: |
          set -euo pipefail
          git config user.name "Pollen Release Bot"
          git config user.email "actions@users.noreply.github.com"
          git add pollen-russia.apk version.json
          if git diff --cached --quiet; then
            echo "Nothing changed"
            exit 0
          fi
          git commit -m "Publish alpha $VERSION_NAME"
          git push
